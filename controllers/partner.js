const controller = {}
const jwt = require('jsonwebtoken')
const bcrypt = require('bcrypt')
const authValidator = require('../validators/auth')


controller.createPartner = async (req, resp) => {
    
}